anthropic>=0.120
mcp[cli]
fastapi
uvicorn
pypdf
keyring

# Local semantic photo search (tools/photo_index.py) — tier B, the small-model
# tier. Optional: nothing in the connector imports these.
# open_clip_torch
# torch
# pillow
# pillow-heif      # iPhone libraries are full of HEIC
# numpy
# reverse_geocoder # offline place names from GPS — never a network lookup
# pycountry        # country codes -> names people actually say
# piexif           # test only: writes EXIF into fixture photos

# Voice enrolment (tools/voice_enrol.py) — recording only, never playback-as-you.
# Optional; the wheel bundles PortAudio on macOS.
# sounddevice
